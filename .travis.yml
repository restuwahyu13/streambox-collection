language: node_js

node_js:
  - node
  - 14
  - 13
  - 12

addons:
  apt:
    packages:
      - make

# before_script:
#   - sudo apt-get update -y
#   - sudo apt-get install -y make

scripts: make build

cache:
  - npm

os:
  - linux
